--Listas!!

head [10,2,3,4] --Nos devuelve el primer numero 
                --return '10'

last [10,2,3,4] --Nos devuelve el ultimo numero
                --return '4'

tail [5,4,3,2,1] --toma una lista y devuelve su cola. En otros palabras, corta la cabeza de la lista.

init [5,4,3,2,1] --toma una lista y devuelve toda la lista excepto su último elemento.

null --comprueba si una lista está vacía. 
     --Si lo está, devuelve True, en caso contrario devuelve False. Usa esta función en lugar de 
     --xs == [] (si tienes una lista que se llame xs).

length
reverse
take
drop
maximum
minimum
sum
concat
product
elem
replicate
:
++
!!